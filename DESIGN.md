# ぬりえアプリ 設計書

3歳児が熱中するクリエイティブな塗り絵 Web アプリの設計書。
インストール不要でブラウザから遊べ、GitHub Pages で公開する。

- 公開 URL（予定）: `https://sakanayuki.github.io/cc_nurie/`
- リポジトリ: `sakanayuki/cc_nurie`

---

## 1. 概要・目的

- 3歳児が **一人で遊びきれる** 塗り絵アプリを作る
- 「塗る → すぐ反応がある → 完成すると絵が動き出す」という体験で熱中させる
- 親のスマホでサッと開ける（インストール不要・縦持ち・オフラインでも壊れない）
- 完全静的サイトとして GitHub Actions → GitHub Pages で自動デプロイする

### 初回リリースのスコープ

| 項目 | 決定 |
|---|---|
| 塗り方 | タップ塗り（主軸）＋ クレヨン自由描画モード |
| クリエイティブ要素 | ① 完成すると絵が動き出す ② マジックブラシ（にじ・キラキラ） |
| 効果音 | なし（将来拡張） |
| デバイス | スマホ優先・縦持ち（タブレットにも対応） |
| 技術 | Vite + TypeScript（フレームワークなし・実行時依存ゼロ） |
| 線画 | どうぶつ・のりもの・自然の3系統、各4点の計12点 |
| 保存 | 塗りかけの localStorage 自動保存のみ（ギャラリーなし） |

---

## 2. ターゲットユーザーと設計原則

主役は3歳児本人。親は「渡す係」であり、遊んでいる間は操作を手伝わない前提で設計する。

3歳児の発達特性から導かれる原則:

| 特性 | 設計原則 |
|---|---|
| 文字が読めない | テキストを一切使わない。全 UI をアイコン・色・形で表現。メニュー階層なし |
| 指先が未発達 | タッチターゲットは最低 60px 四方。多少ズレたタップでも意図した領域に塗れるよう領域は大きめに分割 |
| 集中時間が短い | タップした瞬間に色が付く。ローディングや待ち時間ゼロ。起動から塗り始めまで2タップ以内 |
| 「消えた！」で泣く | 塗りかけは自動保存。全消しなどの破壊的操作は誤タップ不可能な操作（長押し）にする |
| 成功体験が大事 | タップ塗りなら必ず「きれいに」塗れる。はみ出しという失敗が構造的に起きない |

その他の方針:

- **音なし・通知なし・外部通信なし・広告なし**。保護者が安心して渡せること自体を要件とする
- 縦持ちスマホで片手に持たせて遊ばせられるレイアウト
- ピンチズーム・ダブルタップズームは無効化（3歳児は必ず誤発動させるため）

---## 3. 機能要件

### 3.1 画面構成（2画面のみ）

```
[絵選択画面] ←→ [塗り絵画面]
```

- **絵選択画面**: 12点の線画をサムネイルとして 3列×4行 のグリッドで表示。タップで塗り絵画面へ。塗りかけがある絵はその状態のままサムネイル表示される（自分の続きが見つけられる）
  - 12点すべてを1画面に収める（スクロールで隠れた絵は3歳児が発見できないため）。画面の高さから1マスの上限幅を逆算し、縦にはみ出さないようグリッド幅を制限する
  - 並び順は **どうぶつ4点 → のりもの4点 → しぜん4点** の固定順。並び替えや「最近開いた順」は行わない（位置で自分の絵を覚えるため）
- **塗り絵画面**: キャンバス＋パレット。左上の「おうちアイコン」で選択画面へ戻る

ルーティングは URL ハッシュ（`#/` と `#/page/<id>`）で行い、ブラウザバックでも壊れないようにする。

### 3.2 タップ塗り（主軸モード）

- パレットで色を選び、絵の領域をタップするとその領域全体が選択色で塗られる
- 塗り済み領域を別の色でタップし直すと塗り替えられる（何度でもやり直せる）
- タップ時に領域が一瞬ぽよんと弾むフィードバック（CSS transform）を返す

### 3.3 クレヨンモード（自由描画）

- モード切替ボタンでタップ塗りと切り替え
- 指でなぞると太いクレヨン風ストローク（太さ約 12〜16px、端が丸く、ややテクスチャ感のある線）で描ける
- クレヨンは線画の輪郭の「下」に描画され、黒い輪郭線は常にくっきり残る（後述のレイヤー構造）

### 3.4 マジックブラシ（クリエイティブ要素②）

パレットの通常色の並びに「特別な2色」として置く。選ぶだけで使え、モード概念を増やさない。

- **にじいろ**: 塗った領域が虹色グラデーションになる（タップ塗り時）。クレヨン時はストロークに沿って色相が流れる虹色の線になる
- **キラキラ**: 塗った領域がきらめき続ける（明滅する星のパーティクルを CSS アニメーションで重ねる）

### 3.5 完成アニメーション（クリエイティブ要素①）

- 全領域が塗られた瞬間、絵全体がアニメーションを始める（例: 魚が泳ぎだす）＋画面に紙吹雪が舞う
- アニメーションは止まらずループし続け、そのまま塗り直しも可能
- 「完成」の判定はタップ塗りの領域のみで行う（クレヨンは自由要素なので判定に含めない）

### 3.6 自動保存

- 1操作ごとに localStorage へ保存（タップ塗り・クレヨンストローク・アンドゥ含む）
- アプリを閉じても、リロードしても、塗りかけが完全に復元される
- 保存はすべて端末内。サーバー送信は一切なし

### 3.7 やりなおし

- **アンドゥ**: 直前の1操作を取り消すボタン（クレヨンの1ストローク or 直前のタップ塗り）
- **全消し**: その絵をまっさらに戻す。ゴミ箱アイコンの **長押し（1.5秒）** でのみ発動し、長押し中はアイコンにプログレスリングを表示。誤タップでは絶対に発動しない

---

## 4. コンテンツ仕様（線画12点）

線画は本設計に沿って SVG で描き起こす。共通仕様:

- 手描き風の太い輪郭線（stroke 幅 8〜12px 相当、角丸）
- 領域数は **8〜15 領域**（3歳児が飽きる前に塗りきれる粒度）
- 隣接領域は十分な面積を持ち、最小領域でも画面上で指の腹より大きい
- viewBox は縦長 `0 0 600 800` に統一（スマホ縦画面に最適化）

| # | 系統 | id | 題材 | 領域数 | 完成アニメーション |
|---|---|---|---|---|---|
| 1 | どうぶつ | `fish` | さかな | 8 | 体を揺らして泳ぎ、泡がのぼる |
| 2 | どうぶつ | `cat` | ねこ | 12 | しっぽを振り、まばたきする |
| 3 | どうぶつ | `bird` | ことり | 10 | 羽ばたいて上下に飛び、羽根がふわりと舞う |
| 4 | どうぶつ | `elephant` | ぞう | 11 | 鼻を振り、耳をぱたぱた、鼻先から水がぽたぽた落ちる |
| 5 | のりもの | `car` | くるま | 10 | タイヤが回転し、車体が弾んで走る |
| 6 | のりもの | `rocket` | ロケット | 10 | 炎を噴いて上昇し、星が流れる |
| 7 | のりもの | `train` | でんしゃ | 12 | 車輪が回り、線路が流れ、煙突から煙がもくもくのぼる |
| 8 | のりもの | `ship` | ふね | 11 | 波にゆられて船が傾き、かもめが横切る |
| 9 | しぜん | `flower` | おはな | 12 | 風に揺れ、ちょうちょが飛んでくる |
| 10 | しぜん | `sun` | たいようとにじ | 14 | 光線がくるくる回り、にじが輝く |
| 11 | しぜん | `tree` | りんごのき | 13 | 葉がそよぎ、りんごがぽとんと落ちる |
| 12 | しぜん | `moon` | おつきさまとほし | 10 | 星がまたたき、流れ星がすーっと流れる |

完成アニメーションの実装単位は「SVG 内のグループ（`<g>`）に CSS keyframes を適用する」こと。各線画モジュールが自分のアニメ定義を持つ（§6.4）。

線画データはインライン SVG 文字列なので12点でもバンドルは数十KB に収まる。絵選択画面が全点のサムネイルを描くため動的 import による遅延読み込みは行わず、全線画を静的に束ねる。

パーツを塗り領域として切り出すとき、輪郭線レイヤーは fill を持たないので **重なった図形の境界線は隠れず必ず見える**。継ぎ目を消したい場合（ぞうの鼻の付け根・あし・しっぽなど）は、閉じる辺を接続先と同じ円/楕円の弧（`A` コマンド）で描き、輪郭線どうしを重ねる。

---

## 5. 画面・UX 設計（スマホ縦）

### 5.1 塗り絵画面レイアウト

```
┌──────────────────────┐
│ 🏠                   │ ← 戻る（左上・44px。子の親指が届きにくい位置）
│                      │
│                      │
│      キャンバス       │ ← 画面高の約 65〜70%
│   （SVG＋Canvas）     │
│                      │
│                      │
├──────────────────────┤
│  ● ● ● ● ●           │ ← カラーパレット（5列×3段・丸ボタン 64px）
│  ● ● ● ● ●           │    12色＋マジックブラシ2種＝14個を
│  ● ● 🌈 ✨            │    一度に全部見せる（スクロールなし）
├──────────────────────┤
│  [👆タップ] [🖍クレヨン]  ↩  🗑  │ ← モード切替・アンドゥ・全消し(長押し)
└──────────────────────┘
```

- パレット・操作ボタンは画面下部に集約（子どもの親指が自然に届く域）
- 破壊的でない操作（色選び・モード切替）は大きく、破壊的操作（全消し）は端＋長押し
- 選択中の色・モードは白フチ＋拡大表示で明示（文字を使わずに状態を伝える）
- **パレットはスクロールさせない**。3歳児はスクロールで隠れた色を見つけられないため、14個すべてを常時表示する。1マスの大きさを画面幅から算出し（`--swatch`）、そこからパレットの高さを逆算してキャンバスに残す高さを決める。幅に余裕がある画面（508px〜）は 7列×2段に切り替え、余った高さをキャンバスに回す

### 5.2 パレットの12色

赤・オレンジ・黄・黄緑・緑・水色・青・紫・ピンク・茶・グレー・黒。
3歳児の色彩認知に合わせ、彩度高めの「クレヨンの定番色」で構成する。＋にじいろ🌈・キラキラ✨。

### 5.3 表示・操作の細則

- `viewport` は `width=device-width, user-scalable=no`、CSS `touch-action: none`（キャンバス上）でズーム・スクロール誤発動を防止
- 高さは `100dvh` 基準（モバイルブラウザのアドレスバー伸縮に追従）
- 横持ち・タブレットでは、キャンバス左＋パレット右の2カラムに組み替える（CSS メディアクエリのみで対応）。パレットは 3列×5段の縦長になり、ここでもスクロールしない。絵選択画面は同条件で 6列×2行に組み替え、12点が1画面に収まる状態を保つ
- ダークモード対応はしない（塗り絵は白地が正義）

---

## 6. 技術設計

### 6.1 スタック

- **Vite + TypeScript**、UI フレームワークなし
- 実行時依存（dependencies）**ゼロ**。devDependencies は `vite` / `typescript` のみ
- 状態管理は素朴なモジュールスコープ＋カスタムイベントで足りる規模

### 6.2 レイヤー構造（描画の核）

キャンバスは同サイズの3層を重ねる。**重ね順が本アプリの肝**:

```
┌ 上 ┐ SVG 輪郭線レイヤー   … 黒い線画のみ。pointer-events: none
├ 中 ┤ Canvas クレヨンレイヤー … 自由描画ストローク
└ 下 ┘ SVG 塗りレイヤー     … 領域 path の fill を塗る
```

- タップ塗りは下層 SVG の領域 path がタップを受ける
- クレヨンモード時は中層 Canvas が `pointer-events` を受け取る（モードで切替）
- 輪郭線が常に最前面にあるため、クレヨンがはみ出しても絵は破綻しない

### 6.3 ディレクトリ構成

```
cc_nurie/
├ .github/workflows/deploy.yml   … Pages デプロイ
├ index.html
├ vite.config.ts                 … base: '/cc_nurie/'
├ src/
│ ├ main.ts                      … 起動・ハッシュルーティング
│ ├ select-screen.ts             … 絵選択画面
│ ├ paint-screen.ts              … 塗り絵画面の組み立て
│ ├ paint/
│ │ ├ tap-fill.ts                … タップ塗り（領域 fill・ぽよん演出）
│ │ ├ crayon.ts                  … Canvas 自由描画
│ │ ├ magic.ts                   … にじ・キラキラの定義（gradient / particle）
│ │ ├ complete.ts                … 完成判定・完成アニメ起動・紙吹雪
│ │ └ history.ts                 … アンドゥ履歴
│ ├ pages/
│ │ ├ types.ts                   … ColoringPage インターフェース
│ │ ├ index.ts                   … 全12点の登録（系統順の固定配列）
│ │ ├ build.ts                   … パーツ定義から2レイヤーの SVG を組む共通ビルダ
│ │ ├ fish.ts / cat.ts / bird.ts / elephant.ts   … どうぶつ
│ │ ├ car.ts / rocket.ts / train.ts / ship.ts    … のりもの
│ │ ├ flower.ts / sun.ts / tree.ts / moon.ts     … しぜん
│ ├ storage.ts                   … localStorage 読み書き
│ └ style.css
└ DESIGN.md / README.md
```

### 6.4 線画データの共通インターフェース

1線画 = 1 TS モジュール。追加・差し替えが1ファイルで完結する構造にする。

```ts
export interface ColoringPage {
  id: string;                 // 'fish' など。保存キーにも使う
  fillLayerSvg: string;       // 領域 path 群。各 path に data-region="r1" を付与
  outlineLayerSvg: string;    // 輪郭線のみの SVG（fill なし・stroke のみ）
  regionIds: string[];        // 全領域 id。完成判定の分母
  completeAnimationCss: string; // 完成時に注入する CSS keyframes
  // 完成アニメで動かす対象は SVG 側の <g class="anim-..."> に付けたクラスで指定する
}
```

- サムネイルは `fillLayerSvg`＋`outlineLayerSvg` をそのまま縮小表示（保存済みの塗り状態を適用して描画）
- にじいろは共通の `<defs><linearGradient id="magic-rainbow">` を塗りレイヤーに注入し `fill="url(#magic-rainbow)"` で適用
- キラキラは対象領域の `bbox` 内にランダム配置した星 SVG を明滅させるオーバーレイ

### 6.5 保存スキーマ（localStorage）

```
key: "nurie:v1:<pageId>"
value: {
  version: 1,
  regions: { [regionId]: string },  // 色 or "rainbow" | "sparkle"
  crayon: string | null,            // クレヨン層の PNG dataURL
  completed: boolean
}
```

- 書き込みは操作ごと。ただし `crayon` の dataURL 化は重いので、クレヨンはストローク終了（pointerup）時のみ保存
- localStorage 上限（約5MB）対策として、クレヨン層は表示解像度の等倍 PNG に留める
- 読み込み失敗・スキーマ不一致時は握りつぶして「まっさら」から開始（子どもの前でエラー画面を出さない）

### 6.6 完成判定と完成アニメーション

1. タップ塗りのたびに `Object.keys(regions).length === regionIds.length` を判定
2. 初回成立時: `completeAnimationCss` を `<style>` として注入 → SVG ルートに `is-complete` クラス付与 → 紙吹雪（軽量な自前 DOM パーティクル、約2秒）
3. 以降もアニメはループし続け、塗り替えするとその場で色が変わる（アニメは止めない）
4. `completed` フラグを保存し、再訪時も最初から動いている状態で復元する

---

## 7. デプロイ設計（GitHub Pages + GitHub Actions）

### 7.1 方式

- Pages のソースは **GitHub Actions**（ブランチデプロイではない）
- `main` への push をトリガーに、ビルド → 成果物アップロード → デプロイ

### 7.2 ワークフロー（`.github/workflows/deploy.yml`）

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
```

### 7.3 関連設定

- `vite.config.ts` に `base: '/cc_nurie/'`（プロジェクトページのサブパス対応。これを忘れるとアセットが 404 になる）
- **初回のみ手動設定**: リポジトリの Settings → Pages → Source を「GitHub Actions」にする
- SPA だがルーティングはハッシュのみなので 404 フォールバックは不要

---

## 8. 将来拡張（初回スコープ外）

初回リリースでは実装しないが、設計上の受け皿を残しておくもの:

- **効果音**: 塗るたびのポン音＋完成ファンファーレ。Web Audio API の合成音で音声ファイル不要。ミュートボタン（親向け）とセットで導入
- **ギャラリー**: 完成作品の一覧・見返し。保存スキーマの `completed` は既にあるため拡張しやすい
- **スタンプ**: 星・ハートをペタペタ押す第3のモード
- **線画の追加**: `ColoringPage` 準拠の TS モジュールを1つ足すだけで追加可能（§6.4 がその設計）
- **PWA / オフライン対応**: manifest ＋ Service Worker でホーム画面追加・機内モード対応

---

## 9. 実装フェーズの進め方（次のステップ）

1. Vite + TS の骨組み・デプロイワークフロー（空のページが Pages に出るところまで）
2. レイヤー構造＋タップ塗り＋パレット（線画1点 `fish` で成立させる）
3. 自動保存・アンドゥ・全消し
4. クレヨンモード
5. マジックブラシ（にじ・キラキラ）
6. 完成判定・完成アニメーション・紙吹雪
7. 絵選択画面（3列×4行グリッド）＋ 残り11点の線画と各完成アニメ（系統単位で どうぶつ → のりもの → しぜん の順に追加）
8. 実機（スマホ）での子どもテスト → タッチ精度・領域サイズの調整
